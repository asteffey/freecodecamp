{
  "name": "root",
  "private": true,
  "devDependencies": {
    "gh-pages": "^2.1.1",
    "lerna": "^3.16.4"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna run clean",
    "deploy": "lerna run deploy --concurrency 1"
  }
}
